---

- name: OS| install net-tools
  apt:
    name: net-tools
    state: present
- name: OS| install apt-transport-https
  apt:
    name: apt-transport-https
    state: present
- name: OS| install python-pip
  apt:
    name: python-pip
    state: present

# - name: Update resolv.conf
#   lineinfile:
#     path: /etc/resolv.conf
#     regexp: '^nameserver '
#     line: 'nameserver 8.8.8.8'
# - name: Restart service networking
#   service:
#     name: networking
#     state: restarted
#  command: resolvconf -a enp0s3.inet

- name: OS| Install python passlib
  pip:
    name: passlib

- name: OS| Create base directory
  file:
    path: "{{ base_path }}"
    state: directory
    mode: 0775
    owner: root
    group: root

#- name: OS| Create space directory
#  file:
#    path: "{{ base_path }}/{{ item.key }}"
#    state: directory
#    mode: "00755"
#    owner: 'root'
#    group: 'root'
#  loop: "{{ lookup('dict', spaces) }}"